# user-directory
User Directory is a simple React application built using Vite that fetches user data from a public API and displays it in a clean, responsive card layout. It includes loading and error handling states, uses useEffect for API calls, and CSS Grid for responsiveness, demonstrating core React concepts and clean UI design principles.
